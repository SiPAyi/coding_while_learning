coords = (1,2,3)
# we can't modify this but we can access the elements of this tuple if we want

#unpacking in tuples
x = coords[0]
y = coords[1]
z = coords[2]

print(x + y + z)

#instead of doing the above thing we can do this

a, b, c = coords

print(a + b + c)
